# 给 docker compose 用的环境变量（复制为仓库根目录 .env，勿提交）

AI_GATEWAY_API_KEY=
TEXT_API_KEY=
FAL_KEY=
CORS_ORIGINS=*

# 云端 MySQL：若 MySQL 在宿主机，容器内主机一般不是 127.0.0.1
# 例：mysql+pymysql://user:pass@172.17.0.1:3306/lensmith
DATABASE_URL=
JWT_SECRET=
JWT_EXPIRE_MINUTES=10080
