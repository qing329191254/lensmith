# Lensmith Client

Vue 3 + Vite + Pinia + Tailwind (pnpm workspace package).

From repo root:

```bash
pnpm install
pnpm --filter lensmith-client dev
```

Or start everything: `pnpm dev`

Dev server: http://localhost:5173  
`/api` is proxied to FastAPI at `http://127.0.0.1:8000`.
